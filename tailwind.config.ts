import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        background: '#050505',
        surface: '#0f0f0f',
        gold: {
          light: '#e8d2b0',
          DEFAULT: '#b98a4f',
        },
        // Бордовый акцент вместо синего — заменяет всю палитру primary-*
        primary: {
          50: 'rgba(184,38,31,0.12)',
          100: 'rgba(184,38,31,0.20)',
          200: 'rgba(184,38,31,0.35)',
          300: '#e07a68', // светлый акцент — фокус-кольца, hover-бордеры
          400: '#e07a68',
          500: '#b8261f',
          600: '#e07a68', // используется как ЦВЕТ ТЕКСТА ссылок (контраст на тёмном фоне)
          700: '#e8a2a2', // текст на затемнённых бордовых чипах/бейджах
        },
        // Серая шкала перекрашена под тёмную тему — весь сайт использует
        // bg-gray-50 / text-gray-500 / text-gray-900 и т.п., поэтому
        // достаточно переопределить эти значения один раз здесь
        gray: {
          50: '#050505',                     // фон страниц
          100: '#141414',                    // приподнятые поверхности/нейтральные бейджи
          200: '#262626',                    // бордеры
          300: '#4a4a4a',                    // приглушённые иконки-заглушки
          400: '#6b6b6b',                    // приглушённые иконки
          500: 'rgba(255,255,255,0.55)',     // второстепенный текст
          600: 'rgba(255,255,255,0.75)',     // обычный текст
          700: 'rgba(255,255,255,0.85)',     // текст чуть сильнее
          900: '#f4f4f2',                    // заголовки
        },
      },
      borderColor: {
        DEFAULT: '#262626',
      },
      fontFamily: {
        sans: ['Clash Display', 'sans-serif'],
        accent: ['var(--font-fraunces)', 'serif'],
      },
    },
  },
  plugins: [],
}

export default config
